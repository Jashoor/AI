cd backend
Remove-Item .\data\foodflow.db -ErrorAction SilentlyContinue
pip install -r requirements.txt
pip uninstall passlib bcrypt -y    # no longer needed, safe to remove
python -m data.generate_mock_data
python -m app.rag.build_index
uvicorn app.main:app --reload --port 8000
